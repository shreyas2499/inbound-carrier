# Public URL of the adapter service (the Flask app that exposes /otp/peek).
# Leave blank for local dev — the vite dev server proxies /otp to localhost:8000.
# In production set this to the adapter's Railway URL, e.g.
# VITE_ADAPTER_BASE=https://inbound-carrier-adapter.up.railway.app
VITE_ADAPTER_BASE=
